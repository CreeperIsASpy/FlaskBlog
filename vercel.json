{
  "builds": [
    {
      "src": "index.py",
      "use": "@vercel/python",
      "config": {
        "build": "pip install -r requirements.txt && flask init-db"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "index.py"
    }
  ]
}